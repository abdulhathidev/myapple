language: node_js

node_js:
  - 16

cache: npm

install:
  - cd myapple-frontend
  - npm install

script:
  - npm test
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github0token: $GITHUB_TOKEN
  local_dir: myapple-frontend/build
  on:
    branch: main
